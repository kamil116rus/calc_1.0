#include "calc_1.0.h"

#include <stdio.h>
#include <stdlib.h>

//#include "polish_notation.h"
//#include "stack.h"
#include "parser.h"

int main() {
    // char *str;
    // str = calloc(MAX_STR, sizeof(char));
    // scanf("%s", str);
    parser();
    // free(str);
    return 0;
}