#include "calc_1.0.h"

#include <stdio.h>

#include "polish_notation.h"
#include "stack.h"

int main() {
    char *str;
    str = calloc(MAX_STR, sizeof(char));
    i = 0;
    while (str[i] != '\0' || str[i] != '\n') {
    }

    free(str);
    return 0;
}